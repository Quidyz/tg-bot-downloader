[project]
name = "telegram-bot-downloader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiogram>=3.15",
    "aiosqlite>=0.20",
    "pydantic-settings>=2.6",
    "threads-dl",
    "yt-dlp>=2025.1.15",
]

[dependency-groups]
dev = [
    "bandit>=1.9.4",
    "loguru>=0.7.3",
    "mypy>=2.3.1",
    "pre-commit>=4.6.2",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "radon>=6.0.1",
    "ruff>=0.16.8",
]

[tool.uv.sources]
threads-dl = { git = "https://github.com/Quidyz/threads-dl.git" }
